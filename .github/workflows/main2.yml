name: Test ACR Login

on:
  workflow_dispatch:

env:
  ACR_INSTANCE: crpi-30m5pxt0p5qxy6sk.cn-hangzhou.personal.cr.aliyuncs.com

jobs:
  test-login:
    runs-on: ubuntu-latest
    
    steps:
    - name: Test Login with RAM User ID
      run: |
        echo "测试新个人版域名"
        echo "登录到: ${{ env.ACR_INSTANCE }}"
        echo "viJmOV0PANcUFFfgURvbLhQlKAZ22c" | docker login \
          --username "LTAI5tJrCCzD9v4GUmC43kL7@1905267060449036" \
          --password-stdin \
          ${{ env.ACR_INSTANCE }}
        echo "✅ 登录成功！"
